server:
  port: 8080

spring:
  application:
    name: relaxstress-backend

  datasource:
    url: jdbc:postgresql://db.zssrimvwakagaewhpvpe.supabase.co:5432/postgres?sslmode=require
    username: postgres
    password: oEtVig6iFY7ft6Ax
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 5
      minimum-idle: 1
      idle-timeout: 30000
      max-lifetime: 600000
      connection-timeout: 20000

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    com.relaxstress.backend: DEBUG
    org.hibernate.SQL: DEBUG
